# backend/.env

# puerto donde se expone la API dentro del contenedor
PORT=8080

# ruta de la base de datos sqlite (el docker-compose ya monta /data como volumen)
SQLITE_DSN=/data/app.db

# clave secreta para firmar JWT (cambia en prod!)
JWT_SECRET=change-me

# duración de los tokens de acceso (ej: 15m, 1h)
ACCESS_TOKEN_TTL=15m

# duración de los refresh tokens (ej: 7 días)
REFRESH_TOKEN_TTL=168h

# clave AES para cifrar contraseñas del vault (32 bytes → AES-256)
AES_KEY=0123456789abcdef0123456789abcdef
